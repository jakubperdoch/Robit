import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
 ResetPswdScreen_Container: {
  width: '100%',
  flexDirection: 'row',
 },
 ConfirmPswd_Container: {
  width: '100%',
  flexDirection: 'column',
  gap: 20,
 },
});

export default styles;
